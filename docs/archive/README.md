# Historical reference only

- `starting-prompt.md`: unchanged original from `tmp/docs/meta/starting-prompt.md`, archived before replacing that active location with a pointer.
- `old-docs-index.md`: unchanged historical navigation. Its relative links describe the former `tmp/docs/` location.
- The former runtime, Studio, prompts and architecture-specific tests are recoverable at Git base `3abe9692f3b5f3bbe20390a8d8971af994d3f07b`. They are not active imports or default tests.
- `legacy/`, `tmp/` daily artifacts, `data/` sources/run databases and old test database fixtures are preserved. Do not read them by default or delete them during ordinary work.
- Original research brief: `docs/industry-research-brief.md`, preserved as user input, not a runtime instruction file.
- Local-only retired trackers are preserved under `tmp/docs/archive/`; active tracker paths point to the redesign.
