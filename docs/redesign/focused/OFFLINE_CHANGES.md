# Offline correction record

Candidate implements compact original-source presentation, bounded neighboring chunks, source-backed title/issuer annotations, explicit statement-subject caveats, bounded failed-worker handoff, review-original references and the consolidated revision allocation. Generic code contains no issuer names or report-specific source IDs. Company expectations live in `tests/fixtures/redesign/focused/originals.json`; snapshot construction data lives in this report-specific directory.

The existing StageRunner, SourceStore, provider adapters and workflow remain shared. No new role, agent, retry loop or per-claim certification graph was added. The additive ledger subclasses only the existing locked admission/settlement owner, with fixed A/B/C policy in config. Historical LIMITS and RUN_BUDGET.json remain unchanged. The six default stage allocations now sum to the same 1680 seconds plus 120 seconds export; revision receives 180 seconds shifted from research/synthesis. No full run is authorized here.

The original passage API still preserves all stored provenance; model tools use compact views with source handle, URL, exact spans/chunk and header-backed identity. `identity.json` is a small optional source annotation with title/issuer `{value, chunk_id}` entries. Values must occur verbatim in the referenced original. Null identity is explicit when not established. This does not independently certify authorship, and the document issuer is never applied mechanically to statement subjects. Windows open at most two neighboring paragraph/table chunks per side, at most 8000 characters, with no silent truncation.

Research workers reserve up to 60 seconds for final notes. If a provider fails or the deadline/round cap prevents final notes, the original failure remains failed; a separate bounded handoff passes retrieved passages to synthesis as unsynthesized evidence. Complete raw events remain in the worker artifact directory. No model call occurs after each tool result.

Review's material findings now contain original_passages references. The runner checks that quoted text exists in the stated original chunk. That check verifies provenance, not semantic entailment; a reviewer can still misinterpret a genuine quote, which A/B/C must assess. Revision receives the exact draft, brief, findings, sources and figure metadata without duplicated plan/worker notes. It may reject unsupported findings; the preserved false Luxin objection is retained in case C.

Offline validation: 47 accumulated pytest cases pass, including shared queued/active deadlines with fake clocks and slow workers, exact source identity/windows, another company discussed in a document, source caches, SDK quota-event boundary, lost handoff reaching synthesis, objection quote rejection, allowed contested revision, revision writing reserve and short enclosing deadline. Fixture responses test transport and control behavior, not model factual competence. Existing fixture Markdown/PDF tests passed; no real full report was launched. Black passes across 21 Python files. Final pylint result is recorded with the candidate freeze/results.

Actual non-model write probes passed for checkout, .git, data/redesign and the Codex runtime directory. Child CLI read-only evidence isolation and the diagnosed subscription configuration are retained; no filesystem/model probe consumes an attempt. The independent model reviewer still receives figure data and hashes, not native pixels, and cannot establish visual-layout acceptance.
