# Next session — no charged attempts remain

The campaign is stopped at its attempt caps. **10 isolated and 2 full attempts
are consumed. Do not rerun a saved case, reset counters, restart the campaign
clock, or treat unused time/tokens as additional attempts.** Further model work
requires new explicit authorization. The original ledger and all failed work
must remain intact. No background continuation is scheduled.

Permissions are resolved: outer checkout/Git/runtime-state writes and separate
readOnly/no-network child initialization were verified without model calls.
Local commits succeeded. Do not disable intentional evidence isolation to solve
this campaign's remaining quality problems.

Read COMPLETION.md, STAGE2_RESULTS.json and RUNTIME_NOTES.md. The exact final
HEAD/dirty state is in `data/redesign/stage2-final-state.json`. Earlier blocked
completion records and patch are preserved under `stage2-prior-completion/`.

Both full reports remain `draft_review_incomplete`, with known factual errors.
They are useful debugging artifacts, not accepted photomask research. The single
rehearsal revision is more useful but has no independent live recheck. The
separate layout-only derivative improves export readability and intentionally
retains factual errors. See COMPLETION.md for all report/source/figure paths.

If new model work is explicitly authorized, start with the smallest saved
company or reviewer input that reproduces issuer confusion. The final source
inventory includes an exact original identifying passage and asks roles to
verify document identity; the combined change is offline-tested only. Do not
repeat successful planning/industry rehearsals or start another full report
before that concrete failure is understood. In the prior second full run,
later stages read updated prompt text but still used the old inventory; do not
claim it validates the combined representation change.

Verify Qingyi/Luw/Longtu identity against original cover passages before
assigning revenue, project ownership or technical milestones. The independent
reviewer made a false objection by trusting the draft's mistaken issuer label.
Retain bounded review: one review, at most one revision and focused recheck;
no repeat-until-pass or new certification graph. Live recheck and completion
within the revision deadline remain unvalidated. Inspect the saved portable
transcript size and latency before deciding whether a persistent SDK session
would solve a measured problem; no speculative architecture rebuild.

A real Microsoft source corpus and saved service case exist, but service product
quality has not been tested. Actual local embeddings, Claude live behavior and
native-pixel reviewer access also remain unvalidated. Reviewer text/spec/hash
checks do not establish visual readability. Existing source bytes, historical
runs, fixture reports and all failed attempts must remain preserved.
